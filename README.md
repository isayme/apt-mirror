# 选择切换 ubuntu/debian 国内镜像

```bash
bash -c "$(curl -fsSL https://raw.githubusercontent.com/isayme/apt-mirror/HEAD/select.sh)"

# 或

bash -c "$(wget https://raw.githubusercontent.com/isayme/apt-mirror/HEAD/select.sh -O -)"
```
